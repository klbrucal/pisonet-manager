# Desktop Dashboard

WPF and WebView2 desktop host for the browser dashboard.

Set `ServerUrl` in `PisonetManager.Desktop/appsettings.json`, then run:

```powershell
dotnet run --project PisonetManager.Desktop
```
