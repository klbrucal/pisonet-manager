# Server

FastAPI HTTP API, authenticated dashboard sessions, and agent/dashboard WebSocket connections.

Run from this directory:

```powershell
.\.venv\Scripts\Activate.ps1
uvicorn app.main:app --host 0.0.0.0 --port 8000
```
